# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Data — raw downloads are reproducible; processed tables are build artifacts
data/raw/
data/processed/
# soccerdata caches (SOCCERDATA_DIR points into these)
data/soccerdata/
data/understat_cache/

# Local scratch — diagnostic scripts, sample dumps; never committed
scratch/

# site/data.json is a regenerated build artifact (src/export_site_data.py);
# site/index.html embeds a frozen snapshot of it and is the committed deliverable
site/data.json

# Secrets
.env

# OS
Thumbs.db
.DS_Store

# Tooling scratch (seleniumbase / webdriver)
downloaded_files/
latest_logs/
assets/
*.lock

# Unrelated files that OneDrive syncs into this folder — not part of this project
xValue/
*Setup*.exe
*installer*.exe
chromedriver*.exe
models/

# Claude Code worktrees / local
.claude/
