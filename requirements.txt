# Python 3.11 (nodriver + soccerdata's TLS client need it; run everything as `py -3.11 ...`)

pandas>=2.0
numpy>=1.26
requests>=2.28
lxml>=5                 # pandas.read_html over cached FBref / Transfermarkt HTML

scikit-learn>=1.5       # value + outcome models
scipy>=1.11             # Poisson-Skellam, blend weight search

# --- scrapers / ingestion ---
nodriver>=0.50          # FBref + Transfermarkt: pure CDP, no chromedriver binary
                        # (soccerdata's seleniumbase-UC path crashes here - docs/fbref_ingestion.md)
soccerdata>=1.9         # Understat (per-match + per-player xG) and Sofascore readers;
                        # pulls tls-requests (TLS-fingerprint HTTP client)
pyreadr>=0.5            # reads the worldfootballR mirror .rds (advanced stats, TM values 2020-23)
curl_cffi>=0.7          # browser-impersonation fallback used while probing sources

# Optional - only if you get a paid API-Football key (src/pull_api_football.py):
#   nothing extra; it uses `requests`.
