pandas>=2.0
requests>=2.28
lxml>=5              # pandas.read_html parses the cached FBref match-log HTML

# FBref match logs + cup rows (step 1c) and player stats (step 2).
# nodriver = pure Chrome DevTools Protocol, no chromedriver binary. soccerdata's
# seleniumbase-UC path uses a patched uc_driver.exe that CRASHES on this machine
# (see docs/fbref_ingestion.md); nodriver has no such binary and clears FBref's
# Cloudflare challenge reliably. Needs Python 3.11 here (run: py -3.11 ...).
nodriver>=0.50

# worldfootballR data mirror - the advanced (Opta) player stats FBref gates from
# scrapers: xG/xAG, progressive passing, tackles, possession, GCA. big-5,
# 2020-21..2022-23. src/pull_wfr_advanced.py. Reads .rds.
pyreadr>=0.5

# processed CSVs mix latin-1 and (mis-decoded-as-latin-1) utf-8 rows across
# different scraper runs - ftfy repairs the double-encoded ones and leaves
# already-correct text untouched. src/export_site_data.py.
ftfy>=6.1
